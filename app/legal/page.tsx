export default function LegalPage() {
  return (
    <main className="mx-auto max-w-3xl px-4 py-10">
      <h1 className="text-2xl font-semibold">Privacy & Disclaimer</h1>
      <p className="mt-4 text-muted-foreground">
        Prototype only; no personal data is processed. Any IDs entered are discarded immediately and never sent to real
        services. Replace the mock API with real integrations later if you choose to build this for production use.
      </p>
    </main>
  )
}
